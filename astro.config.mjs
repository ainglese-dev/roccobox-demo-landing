// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ainglese-dev.github.io',
  // Uncomment base for production deployment:
  // base: '/roccobox-demo-landing',
});
